#!/bin/sh
set -e

# Pre-remove hook for ai-distro-voice
if command -v systemctl >/dev/null 2>&1; then
  systemctl stop ai-distro-voice.service || true
  systemctl disable ai-distro-voice.service || true
fi

exit 0
