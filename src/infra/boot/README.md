# Boot Assets (Scaffold)

This directory holds bootloader configs for ISO builds.

- `grub/` GRUB config for BIOS/UEFI
- `efi/` EFI boot assets (placeholder)
- `syslinux/` legacy BIOS boot assets (placeholder)

Update paths as rootfs layout is finalized.
