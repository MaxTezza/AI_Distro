# ISO Build

This directory will hold ISO build assets and scripts.

Planned layout:
- `live/` live system rootfs build
- `netinst/` net installer assets
- `profiles/` distro profile definitions
- `hooks/` post‑install hooks
