[package]
name = "ai-distro-agent"
version = "0.1.0"
edition = "2021"

[dependencies]
ai-distro-common = { path = "../ai-distro-common" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
log = "0.4"
